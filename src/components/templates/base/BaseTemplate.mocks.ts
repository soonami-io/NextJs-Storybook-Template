import { IBaseTemplate } from './BaseTemplate';

const base: IBaseTemplate = {
  sampleTextProp: 'Hello world!',
};

const base_alt: IBaseTemplate = {
  sampleTextProp: 'Bonjour!',
};

export const mockBaseTemplateProps = {
  base,
  base_alt,
};
